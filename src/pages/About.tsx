import { Container } from "../components";
import { H1, Button } from "@blueprintjs/core"

export const About = () => {

  return (
    <Container>
      <Button
        intent='success'
        text='Ok'
        onClick={() => {

        }}
      />
    </Container>
  );
}

export default About